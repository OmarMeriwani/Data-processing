CREATE TABLE [dbo].[MaxMonths] (
    [Country] INT NOT NULL,
    [Year]    INT            NOT NULL,
    [Month]   NVARCHAR (100) NOT NULL,
    [Births]  INT            NOT NULL,
    [id]      INT            NOT NULL
);

